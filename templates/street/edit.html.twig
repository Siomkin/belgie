{% extends 'layout.html.twig' %}

{% block content -%}
    <h1>Редактировать улицу</h1>

    {{ include('street/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('street') }}">back to list</a>

    <div class="text-right">{{ include('street/_delete_form.html.twig') }}</div>
{% endblock %}
