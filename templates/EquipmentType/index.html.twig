{% extends 'layout.html.twig' %}

{% block content -%}
    <h1>Список типов оборудования</h1>
    <div class="text-right">
        <a class="btn btn-default" href="{{ path('equipmenttype_new') }}">
            Добавить новый тип
        </a>
    </div>
    <table class="table">
        <thead>
        <tr>
            <th>Id</th>
            <th>Название</th>

            <th>Действия</th>
        </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td>{{ entity.id }}</td>
                <td><a href="{{ path('equipmenttype_show', { 'id': entity.id }) }}">{{ entity.name }}</a></td>
                <td><a href="{{ path('equipmenttype_edit', { 'id': entity.id }) }}">редактировать</a></td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
