{% extends 'layout.html.twig' %}

{% block content -%}
    <h1>Ports creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('ports') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
