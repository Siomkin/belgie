{% extends 'layout.html.twig' %}

{% block content -%}
    <h1>Список организаций</h1>
    <div class="text-right">
        <a class="btn btn-default" href="{{ path('organization_new') }}">
            Добавить организацию
        </a>
    </div>
    <table class="table">
        <thead>
        <tr>
            <th>Id</th>
            <th>Название</th>
            <th>Действия</th>
            <th>Данные</th>
        </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('organization_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.name }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('organization_equipments', { 'id': entity.id }) }}">Список оборудования</a>
                        </li>
                        <li>
                            <a href="{{ path('organization_lines', { 'id': entity.id }) }}">Список линий связи</a>
                        </li>
                        <li>
                            <a href="{{ path('organization_canals', { 'id': entity.id }) }}">Список каналов связи</a>
                        </li>
                        <li>
                            <a href="{{ path('organization_edit', { 'id': entity.id }) }}">Редактировать</a>
                        </li>
                    </ul>
                </td>
                <td>
                    <li>
                        <a href="{{ path('organization_download', { 'id': entity.id }) }}">Скачать данные</a>
                    </li>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}
