{% extends 'layout.html.twig' %}

{% block content -%}
    <h1>Тип оборудования</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Название</th>
            <td>{{ equipment_type.name }}</td>
        </tr>
        <tr>
            <th>Id</th>
            <td>{{ equipment_type.id }}</td>
        </tr>
        </tbody>
    </table>

    <ul class="record_actions">
        <li>
            <a href="{{ path('equipment_type_index') }}">
                Назад к списку
            </a>
        </li>
        <li>
            <a href="{{ path('equipment_type_edit', { 'id': equipment_type.id }) }}">
                Редактировать
            </a>
        </li>
    </ul>
    <div class="text-right"> {{ include('equipment_type/_delete_form.html.twig') }}</div>

{% endblock %}