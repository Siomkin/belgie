{% extends 'layout.html.twig' %}

{% block content -%}
    <h1>Редактировать тип оборудования</h1>

    {{ include('equipment_type/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('equipment_type_index') }}">{{ 'back_to_list'|trans }}</a>

    <div class="text-right"> {{ include('equipment_type/_delete_form.html.twig') }}</div>
{% endblock %}
