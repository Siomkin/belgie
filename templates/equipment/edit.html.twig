{% extends 'layout.html.twig' %}

{% block content -%}
    <h1>Редактировать оборудование</h1>

    {{ include('equipment/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('equipment_index') }}">{{ 'back_to_list'|trans }}</a>

    <div class="text-right"> {{ include('equipment/_delete_form.html.twig') }}</div>
{% endblock %}
