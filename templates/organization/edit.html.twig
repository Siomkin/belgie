{% extends 'layout.html.twig' %}

{% block content -%}
    <h1>Редактировать организацию</h1>

    {{ include('organization/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('organization_index') }}">{{ 'back_to_list'|trans }}</a>

    <div class="text-right">{{ include('organization/_delete_form.html.twig') }}</div>
{% endblock %}
