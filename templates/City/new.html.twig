{% extends 'layout.html.twig' %}

{% block content -%}
    <h1>Добавление города</h1>

    {{ form(form) }}

    <ul class="record_actions">
        <li>
            <a href="{{ path('city') }}">
                Назад к списку
            </a>
        </li>
    </ul>
{% endblock %}
