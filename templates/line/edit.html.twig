{% extends 'layout.html.twig' %}

{% block content -%}
    <h1>Редактировать линию</h1>

    {{ include('line/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('line_index') }}">back to list</a>

    <div class="text-right"> {{ include('line/_delete_form.html.twig') }}</div>
{% endblock %}
