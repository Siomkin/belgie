{% extends 'AppBundle::layout.html.twig' %}

{% block content -%}
    <h1>Города</h1>
    <div class="text-right">
        <a class="btn btn-default" href="{{ path('city_new') }}">
            Добавить город
        </a>
    </div>
    <table class="table">
        <thead>
        <tr>
            <th>Id</th>
            <th>Название</th>
            <th>Действия</th>
        </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td>{{ entity.id }}</td>
                <td><a href="{{ path('city_show', { 'id': entity.id }) }}">{{ entity.name }}</a></td>
                <td>
                    <a href="{{ path('city_edit', { 'id': entity.id }) }}">Редактировать</a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
