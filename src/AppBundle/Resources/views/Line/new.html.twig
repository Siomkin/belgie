{% extends 'AppBundle::layout.html.twig' %}

{% block content -%}
    <h1>Line creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('line') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
