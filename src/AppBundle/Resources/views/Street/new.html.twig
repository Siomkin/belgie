{% extends 'AppBundle::layout.html.twig' %}

{% block content -%}
    <h1>Добавление улицы</h1>

    {{ form(form) }}

    <ul class="record_actions">
        <li>
            <a href="{{ path('street') }}">
                Назад к списку
            </a>
        </li>
    </ul>
{% endblock %}
